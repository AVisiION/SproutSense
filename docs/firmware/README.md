# Firmware docs

Firmware and hardware documentation for device firmware. For flashing, pin mappings, and telemetry contracts see `docs/firmware/firmware-guide.md` and `docs/firmware/wiring-guide.md`.

Primary firmware locations: `firmware/ESP32-SENSOR` and `firmware/ESP32_CAM_AI`.
