# Render deployment (backend)

Notes and a backup `render.yaml` for deploying the backend to Render. Use the Render dashboard or the Blueprint import to deploy `apps/api`.

Required environment variables include `MONGODB_URI` and `CORS_ORIGIN`. See `docs/operations/render-deployment.md` and `docs/operations/deployment-checklist.md` for complete deployment and health-check instructions.