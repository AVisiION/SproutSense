/**
 * RecordsPage.jsx — pages/Records/
 * Sensor history table — shows paginated sensor readings from MongoDB.
 *
 * Re-exports the original implementation to avoid code duplication.
 * All business logic lives in pages/RecordsPage.jsx.
 */
import './RecordsPage.css';
export { default } from '../RecordsPage';
