/**
 * ScrollVelocity.jsx — bits/
 * Reusable parallax scroll-velocity text animation component (react-bits style).
 * Used on the HomePage hero section.
 */
export { ScrollVelocity } from '../ScrollVelocity';
