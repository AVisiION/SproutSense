/**
 * ElasticSlider.jsx — bits/
 * Reusable animated elastic slider UI component (react-bits style).
 * Used in ControlCard for moisture threshold input.
 */
export { ElasticSlider } from '../ElasticSlider';
